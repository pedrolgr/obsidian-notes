
[[TypeScript]] includes several **primitive types** that represent the most basic data types. These types are **immutable** and hold simple values.

### 🔹 Common Primitive Types

- `string` – Textual data  
```ts
let name: string = "Alice";
```

- `number` – All numbers (integers and floats)  
```ts
let age: number = 25;
```

- `boolean` – True or false values  
```ts
let isActive: boolean = true;
```

- `null` – Explicitly no value  
```ts
let data: null = null;
```

- `undefined` – A variable declared but not assigned  
```ts
let value: undefined = undefined;
```

- `bigint` – Arbitrarily large integers  
```ts
let big: bigint = 9007199254740991n;
```

- `symbol` – Unique identifiers  
```ts
let sym: symbol = Symbol("id");
```
