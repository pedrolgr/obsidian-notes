Means that variables created inside a block scope (inside `function` or `if/else`for exemple) can be used just inside this scope.



Declarations like [[const]], [[let]] are only accessible inside this block.