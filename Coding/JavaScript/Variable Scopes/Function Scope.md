Variables created inside functions has function scope, they can just be used inside functions.

Variables declared like [[var]], [[let]], [[const]] are similar when is declared in [[Function Scope]].

Variables declared like [[var]] inside a function can't be accessible outside, because it is hoisted to the top of the function and not outside of it.