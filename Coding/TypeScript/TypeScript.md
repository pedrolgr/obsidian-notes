Typescript is a supertset of [[JavaScript]], all [[JavaScript]] code is a valid TypeScript code, but not even all TypeScript code is a valid [[JavaScript]] code.

Typescript catches a ton of bugs and errors at compile time