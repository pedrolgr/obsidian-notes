
On [[TypeScript]] can set a implicit type to a variable or explicit type:


```Typescript
	// IMPLICIT
	let x = 2;  // THE COMPILER WILL UNDERSTAND X AS A NUMBER TYPE BECAUSE OF HIS FIRST VALUE
	
	x = "test"; // COMPILER WILL NOT ACCEPT IT

	// EXPLICIT
	let y: number = 2;
```
